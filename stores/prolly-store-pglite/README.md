# prolly-store-pglite

PGlite sidecar storage adapter for
[`prolly-map`](https://crates.io/crates/prolly-map). The adapter launches a
Node.js sidecar with `@electric-sql/pglite` available in its working directory.

```toml
[dependencies]
prolly-map = "0.2"
prolly-store-pglite = "0.1"
```
