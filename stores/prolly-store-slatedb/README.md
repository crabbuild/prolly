# prolly-store-slatedb

SlateDB storage adapter for [`prolly-map`](https://crates.io/crates/prolly-map).
It accepts SlateDB-compatible object stores and exposes the synchronous prolly
store contract.

```toml
[dependencies]
prolly-map = "0.2"
prolly-store-slatedb = "0.1"
```
