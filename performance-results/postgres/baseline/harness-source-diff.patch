diff --git a/benchmarks/postgres-scale/Cargo.lock b/benchmarks/postgres-scale/Cargo.lock
index 16af8626..6b9190f5 100644
--- a/benchmarks/postgres-scale/Cargo.lock
+++ b/benchmarks/postgres-scale/Cargo.lock
@@ -987,0 +988,10 @@ dependencies = [
+ "sha2-asm",
+]
+
+[[package]]
+name = "sha2-asm"
+version = "0.6.4"
+source = "registry+https://github.com/rust-lang/crates.io-index"
+checksum = "b845214d6175804686b2bd482bcffe96651bb2d1200742b712003504a2dac1ab"
+dependencies = [
+ "cc",
diff --git a/benchmarks/postgres-scale/src/cli.rs b/benchmarks/postgres-scale/src/cli.rs
index 14c986e4..9d516660 100644
--- a/benchmarks/postgres-scale/src/cli.rs
+++ b/benchmarks/postgres-scale/src/cli.rs
@@ -6 +6 @@ use crate::model::{Operation, Pattern};
-pub const USAGE: &str = "usage: prolly-postgres-scale-bench [--profile smoke|full] [--url URL] [--output PATH] [--revision REV] [--dirty|--clean] [--sizes LIST] [--runs N] [--operations LIST] [--patterns LIST] [--changes N|auto] [--min-free-gb N]";
+pub const USAGE: &str = "usage: prolly-postgres-scale-bench [--profile smoke|full] [--url URL] [--output PATH] [--revision REV] [--dirty|--clean] [--sizes LIST] [--runs N] [--operations LIST] [--patterns LIST] [--changes N|auto] [--read-samples N] [--min-free-gb N]";
@@ -18,0 +19 @@ pub struct RunConfig {
+    pub read_samples: usize,
@@ -33,0 +35 @@ impl RunConfig {
+            read_samples: 100,
@@ -42,0 +45 @@ impl RunConfig {
+            read_samples: 10_000,
@@ -59,2 +62,7 @@ impl RunConfig {
-        if self.changes == Some(0) {
-            return Err("changes must be positive".to_string());
+        if self.changes == Some(0) || self.read_samples == 0 {
+            return Err("changes and read samples must be positive".to_string());
+        }
+        if self.operations.contains(&Operation::Merge)
+            && self.changes.is_some_and(|changes| changes % 2 != 0)
+        {
+            return Err("merge requires an even total change count".to_string());
@@ -125,0 +134,3 @@ where
+            "--read-samples" => {
+                config.read_samples = parse_number(&take(&overrides, &mut index, flag)?, flag)?;
+            }
@@ -176,0 +188 @@ mod tests {
+        assert_eq!(config.read_samples, 10_000);
@@ -196,0 +209,2 @@ mod tests {
+            "--read-samples",
+            "10",
@@ -201,0 +216 @@ mod tests {
+        assert_eq!(config.read_samples, 10);
diff --git a/benchmarks/postgres-scale/src/harness.rs b/benchmarks/postgres-scale/src/harness.rs
index 8f8d078a..63906564 100644
--- a/benchmarks/postgres-scale/src/harness.rs
+++ b/benchmarks/postgres-scale/src/harness.rs
@@ -34,0 +35 @@ pub fn enumerate_cells(config: &RunConfig, records: usize) -> Vec<CellSpec> {
+                        read_samples: config.read_samples,
@@ -47,0 +49 @@ pub fn enumerate_cells(config: &RunConfig, records: usize) -> Vec<CellSpec> {
+                    read_samples: config.read_samples,
@@ -222 +224,3 @@ mod tests {
-        let config = RunConfig::smoke();
+        let mut config = RunConfig::smoke();
+        config.changes = Some(300);
+        config.read_samples = 100;
@@ -238,0 +243,2 @@ mod tests {
+        assert!(cells.iter().all(|cell| cell.changes == 300));
+        assert!(cells.iter().all(|cell| cell.read_samples == 100));
diff --git a/benchmarks/postgres-scale/src/model.rs b/benchmarks/postgres-scale/src/model.rs
index 2d0eb5ed..78cd80c9 100644
--- a/benchmarks/postgres-scale/src/model.rs
+++ b/benchmarks/postgres-scale/src/model.rs
@@ -131,0 +132 @@ pub fn merge_ids(records: usize, count: usize, pattern: Pattern) -> (Vec<usize>,
+    let branch_count = count / 2;
@@ -134,3 +135,2 @@ pub fn merge_ids(records: usize, count: usize, pattern: Pattern) -> (Vec<usize>,
-            (records..records.saturating_add(count)).collect(),
-            (records.saturating_add(count)..records.saturating_add(count.saturating_mul(2)))
-                .collect(),
+            (records..records.saturating_add(branch_count)).collect(),
+            (records.saturating_add(branch_count)..records.saturating_add(count)).collect(),
@@ -139,2 +139,11 @@ pub fn merge_ids(records: usize, count: usize, pattern: Pattern) -> (Vec<usize>,
-            let ids = random_ids(records, count.saturating_mul(2), 0x006d_6572_6765);
-            (ids[..count].to_vec(), ids[count..].to_vec())
+            let ids = random_ids(records, count, 0x006d_6572_6765);
+            let mut left = Vec::with_capacity(branch_count);
+            let mut right = Vec::with_capacity(branch_count);
+            for (index, id) in ids.into_iter().enumerate() {
+                if index % 2 == 0 {
+                    left.push(id);
+                } else {
+                    right.push(id);
+                }
+            }
+            (left, right)
@@ -143,2 +152,2 @@ pub fn merge_ids(records: usize, count: usize, pattern: Pattern) -> (Vec<usize>,
-            let ids = pattern_ids(records, count.saturating_mul(2), pattern, 0);
-            (ids[..count].to_vec(), ids[count..].to_vec())
+            let ids = pattern_ids(records, count, pattern, 0);
+            (ids[..branch_count].to_vec(), ids[branch_count..].to_vec())
@@ -213 +222 @@ mod tests {
-    fn merge_branches_are_disjoint_for_every_pattern() {
+    fn merge_total_is_split_evenly_into_disjoint_branches() {
@@ -216,2 +225,2 @@ mod tests {
-            assert_eq!(left.len(), 1_000);
-            assert_eq!(right.len(), 1_000);
+            assert_eq!(left.len(), 500);
+            assert_eq!(right.len(), 500);
@@ -220,0 +230,13 @@ mod tests {
+
+    #[test]
+    fn random_merge_branches_are_interleaved_across_the_keyspace() {
+        let (left, right) = merge_ids(100_000, 1_000, Pattern::Random);
+        let left = left.into_iter().collect::<BTreeSet<_>>();
+        let right = right.into_iter().collect::<BTreeSet<_>>();
+        let combined = left.union(&right).copied().collect::<Vec<_>>();
+        let transitions = combined
+            .windows(2)
+            .filter(|pair| left.contains(&pair[0]) != left.contains(&pair[1]))
+            .count();
+        assert!(transitions > 900, "only {transitions} branch transitions");
+    }
diff --git a/benchmarks/postgres-scale/src/workloads.rs b/benchmarks/postgres-scale/src/workloads.rs
index 872f24c0..c6796b70 100644
--- a/benchmarks/postgres-scale/src/workloads.rs
+++ b/benchmarks/postgres-scale/src/workloads.rs
@@ -29,0 +30 @@ pub struct CellSpec {
+    pub read_samples: usize,
@@ -280 +281 @@ async fn run_get(
-    let ids = lookup_ids(fixture.records, spec.changes, spec.pattern);
+    let ids = lookup_ids(fixture.records, spec.read_samples, spec.pattern);
@@ -349 +350 @@ async fn run_query(
-    let ids = lookup_ids(fixture.records, spec.changes, spec.pattern);
+    let ids = lookup_ids(fixture.records, spec.read_samples, spec.pattern);
@@ -409 +410 @@ async fn run_scan(
-        let ids = lookup_ids(fixture.records, spec.changes, spec.pattern);
+        let ids = lookup_ids(fixture.records, spec.read_samples, spec.pattern);
@@ -539,0 +541,3 @@ async fn run_merge(
+    if spec.changes % 2 != 0 {
+        return Err("merge requires an even total change count".to_string());
+    }
@@ -859,0 +864 @@ mod tests {
+                        read_samples: 100,
@@ -875,0 +881 @@ mod tests {
+                read_samples: 100,
diff --git a/scripts/run_postgres_scale_benchmark.sh b/scripts/run_postgres_scale_benchmark.sh
index 84c0c48f..116df32f 100755
--- a/scripts/run_postgres_scale_benchmark.sh
+++ b/scripts/run_postgres_scale_benchmark.sh
@@ -35,0 +36 @@ case "$PROFILE" in
+    READ_SAMPLES="${BENCH_READ_SAMPLES:-100}"
@@ -40,0 +42 @@ case "$PROFILE" in
+    READ_SAMPLES="${BENCH_READ_SAMPLES:-10000}"
@@ -103,0 +106,3 @@ fi
+  printf 'read_samples=%s\n' "$READ_SAMPLES"
+  printf 'merge_changes_semantics=total_split_evenly\n'
+  printf 'random_merge_branch_distribution=interleaved\n'
@@ -132,0 +138 @@ ARGS=(
+  --read-samples "$READ_SAMPLES"
diff --git a/scripts/summarize_postgres_scale_benchmark.py b/scripts/summarize_postgres_scale_benchmark.py
index 0cdecd2a..9ab856a9 100755
--- a/scripts/summarize_postgres_scale_benchmark.py
+++ b/scripts/summarize_postgres_scale_benchmark.py
@@ -96 +96,2 @@ def aggregate(rows):
-def render_report(summaries, rows):
+def render_report(summaries, rows, manifest=None):
+    manifest = manifest or {}
@@ -98,0 +100,2 @@ def render_report(summaries, rows):
+    changes = int(manifest.get("changes", "0")) if manifest.get("changes", "").isdigit() else 0
+    read_samples = int(manifest.get("read_samples", "0")) if manifest.get("read_samples", "").isdigit() else 0
@@ -106,0 +110,11 @@ def render_report(summaries, rows):
+    if changes and read_samples:
+        lines.extend(
+            [
+                "## Workload cardinality",
+                "",
+                f"Batch and diff mutate {changes:,} keys. Point get, multi-get, and bounded scan sample {read_samples:,} keys or entries.",
+                f"Merge treats {changes:,} as the total change count: {changes // 2:,} changes per branch across two disjoint branches.",
+                "Random merge keys are interleaved across both branches so each branch spans the full base keyspace.",
+                "",
+            ]
+        )
@@ -211 +225,3 @@ def summarize(input_path, manifest_path, output_dir, allow_partial=False):
-    (output_dir / "report.md").write_text(render_report(summaries, rows), encoding="utf-8")
+    (output_dir / "report.md").write_text(
+        render_report(summaries, rows, manifest), encoding="utf-8"
+    )
diff --git a/scripts/tests/test_summarize_postgres_scale_benchmark.py b/scripts/tests/test_summarize_postgres_scale_benchmark.py
index 330e634d..0ca75e1d 100644
--- a/scripts/tests/test_summarize_postgres_scale_benchmark.py
+++ b/scripts/tests/test_summarize_postgres_scale_benchmark.py
@@ -65 +65,10 @@ class SummarizerTests(unittest.TestCase):
-        report = module.render_report(module.aggregate([row(1, 1000)]), [row(1, 1000)])
+        report = module.render_report(
+            module.aggregate([row(1, 1000)]),
+            [row(1, 1000)],
+            {
+                "changes": "300000",
+                "read_samples": "10000",
+                "merge_changes_semantics": "total_split_evenly",
+                "random_merge_branch_distribution": "interleaved",
+            },
+        )
@@ -68,0 +78,4 @@ class SummarizerTests(unittest.TestCase):
+        self.assertIn("300,000", report)
+        self.assertIn("10,000", report)
+        self.assertIn("150,000 changes per branch", report)
+        self.assertIn("interleaved across both branches", report)
